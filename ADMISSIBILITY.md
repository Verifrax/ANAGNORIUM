# Admissibility

A claim is admissible for ANAGNORIUM only if:

- it belongs to a declared claim class
- its law basis is explicit
- its epoch basis is explicit
- its input objects are inspectable
- its historical status is not misread as active current truth
- its recognition scope is bounded

Inadmissible inputs must be rejected explicitly rather than silently carried forward.
